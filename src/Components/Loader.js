import React from "react";
import styled from "styled-components";

import "./loader.css"

const Loader = () => {
  return (
    <div id="loader"></div> 
  );
};

export default Loader;


